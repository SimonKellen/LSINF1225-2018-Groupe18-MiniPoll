<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.lsinf1225.minipoll">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="be.lsinf1225.minipoll.Classes.MiniPollApp">

        <activity android:name=".Activites.ConnexionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activites.MenuPrincipalActivity"/>

        <activity android:name=".Activites.GestionAmisActivity"/>

        <activity android:name=".Activites.CreationCompteActivity"/>

        <activity android:name=".Activites.CreationSondageActivity"/>

        <activity android:name=".Activites.MenuAccordActivity"/>

        <activity android:name=".Activites.MenuChoixActivity"/>

        <activity android:name=".Activites.MenuQuestionnaireActivity"/>

        <activity android:name=".Activites.CreationProfilActivity"/>

        <activity android:name=".Activites.ProfilActivity"/>

    </application>

</manifest>